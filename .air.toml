root = "."
tmp_dir = "tmp"

[build]
  cmd = "go mod download && go build -buildvcs=false -o ./tmp/api ./cmd/api"
  entrypoint = "./tmp/api"
  include_ext = ["go"]
  exclude_dir = ["tmp", "tmp_consumer", "tmp_outbox", "tmp_webhook", "tmp_notification", "tmp_audit", "vendor"]
  delay = 500
  stop_on_error = true

[log]
  time = false

[misc]
  clean_on_exit = true
